/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PhotogroupItemComponent } from './photogroup-item.component';

describe('Component: PhotogroupItem', () => {
  it('should create an instance', () => {
    let component = new PhotogroupItemComponent();
    expect(component).toBeTruthy();
  });
});
