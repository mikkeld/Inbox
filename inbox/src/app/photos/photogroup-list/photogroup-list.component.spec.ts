/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PhotogroupListComponent } from './photogroup-list.component';

describe('Component: PhotogroupList', () => {
  it('should create an instance', () => {
    let component = new PhotogroupListComponent();
    expect(component).toBeTruthy();
  });
});
